<%= form_for [@post, @comment], remote: true do |f| %>
  <p>
   <%= f.label :body, "New comment" %><br/>
   <%= f.text_area :body, id: "comment_body" %>
 </p>
 <p><%= f.submit "Add comment", id: 'add_comment_form_button' %></p> 
 <% end %>